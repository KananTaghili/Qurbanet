# Expo HAS CHANGED

This project is pinned to Expo SDK 54 (to match the physical test device's Expo Go version). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code, and do not upgrade the `expo` package or its aligned dependencies without being asked.
